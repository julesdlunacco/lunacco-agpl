/**
 * ImageUploader — media picker for uploading images via the WordPress media library.
 *
 * Phase 3 stub — full implementation in Phase 3.3.
 */
import React from 'react';

export default function ImageUploader( { onUpload, label = 'Upload Image' } ) {
  return null;
}
